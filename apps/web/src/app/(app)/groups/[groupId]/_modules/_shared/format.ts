// Compact timestamp options shared with <LocalTime> so client-rendered times match the old look.
export const COMPACT_DATE: Intl.DateTimeFormatOptions = { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" };
