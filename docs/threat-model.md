# Threat Model

Commons assumes risks from over-collected data, admin overreach, plugin misuse, coercive roles, trust capture, exposed guest requests, lost offline devices, and federation metadata leaks.

Early controls should include minimal retention, encrypted sensitive fields, encrypted offline storage, permission checks, privileged action audit logs, scoped trust, expiring roles, export and deletion workflows, and no global reputation scoring.
